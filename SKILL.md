---
name: "repo-security-audit"
description: "对指定代码仓库做纯静态白盒安全审计：按密钥泄露、注入、认证授权、密码学、供应链等 10 个安全维度逐项排查，产出 HTML + JSON 报告。当用户要求审计/评估某仓库或代码目录的安全性、安全巡检、漏洞排查、上线前安全把关时调用。"
---

# 仓库安全审计（Repo Security Audit）

对用户指定的代码仓库执行**纯静态白盒安全审计**：只通过阅读代码、配置、依赖清单与目录结构进行推理判断，**不执行**目标仓库的代码，**不依赖**任何外部扫描工具（Semgrep / gitleaks / npm audit / trivy 等一律不作为前提）。

审计完成后产出两份文件：一份给人看的单文件 HTML 报告，一份给机器读的 JSON 结果。

## 何时使用

**应该使用：**
- 用户要求「审计 / 评估 / 检查 / 排查」某个仓库或目录的安全性
- 上线前、并购尽调、外包验收、开源组件选型前的安全把关
- 用户指定了一个本地路径或仓库名，并询问「有没有安全问题」

**不应使用：**
- 目标只是一个代码片段或单个函数（直接回答即可，无需走完整流程）
- 用户要求的是渗透测试、动态扫描、运行时漏洞利用（本 skill 只做静态审计）
- 用户要求的是修复代码（本 skill 默认只出报告，修复需用户另行确认）

## 目录结构

所有审计知识与规范存放在与本文件同级的 `prompts/` 目录：

```
<仓库根>/
├── SKILL.md                      ← 本文件，技能入口
└── prompts/
    ├── 00-index.md               ← 维度总览、优先级、编排策略
    ├── 01-secrets-and-credentials.md
    ├── 02-injection.md
    ├── 03-authn-authz.md
    ├── 04-cryptography.md
    ├── 05-dependency-supply-chain.md
    ├── 06-sensitive-data-exposure.md
    ├── 07-config-and-infrastructure.md
    ├── 08-business-logic-and-race-conditions.md
    ├── 09-file-and-path-handling.md
    ├── 10-logging-error-handling.md
    └── 11-report-spec.md         ← JSON schema 与 HTML 报告规范
```

**执行前必须先读 `prompts/00-index.md`**，再按需读取具体维度文件。不要跳过索引直接凭印象审计。

## 输入参数

| 参数 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| 目标仓库路径 | 是 | 无 | 本地绝对路径，或可从当前工作区推断出的仓库 |
| 审计维度 | 否 | 全部 10 个安全维度 | 用户可指定只跑某几个维度 |
| 输出目录 | 否 | 目标仓库根目录 | 报告文件名固定为 `security-audit-report.html` / `.json` |
| 审计深度 | 否 | 标准 | 快扫（只跑高危维度 + 抽样）/ 标准 / 深度（全量逐文件） |

用户信息缺失时：路径可推断则自行推断并声明；维度、输出目录、深度按默认值执行，并在报告 `meta` 中注明。

## 执行流程

### 第 0 步：确认范围与侦察

1. 确认目标路径存在，读取 `README`、包管理文件（`package.json` / `pom.xml` / `go.mod` / `requirements.txt` / `Cargo.toml` 等）判断技术栈。
2. 统计规模：文件数、代码行数、主要语言分布。
3. 识别**审计边界**：标记哪些目录/文件不纳入（`node_modules`、`vendor`、`dist`、`build`、`*.min.js`、测试数据、生成代码、二进制文件）。边界必须写入报告 `meta.scope`。
4. 识别**入口面**：Web 路由、API 控制器、CLI 入口、定时任务、消息消费者、gRPC/GraphQL 定义。这些是后续追踪数据流的主线。
5. 识别**信任边界**：用户输入从哪里进来（HTTP 参数、Header、Cookie、文件上传、消息体、第三方回调），在哪里被使用。

### 第 1 步：逐维度审计

按 `prompts/00-index.md` 给出的优先级顺序遍历维度。对每个维度：

1. 读取对应的 `prompts/NN-*.md`，按其「检索线索」章节执行 Grep/Glob 定位候选点。
2. 对每个候选点，**打开真实代码上下文**（前后各若干十行）做人工判定，区分「真漏洞」与「误报」。
3. 按该维度文件的「判定标准」确认是否成立，按「分级参考」初步定级。
4. 记录发现：必须包含真实文件路径、行号区间、可复现的代码证据。

**一个维度审完立即记录，不要攒到最后凭记忆汇总。**

### 第 2 步：数据流串联

单一维度的点状发现不足以定级。对高危候选点，向上追溯污点源（Source）、向下追踪污点汇（Sink）：

- 是否存在**未经过滤的完整链路**？有链路 → 提升置信度与等级。
- 是否存在**中间拦截**（参数校验、白名单、ORM 参数化、转义、权限中间件）？有拦截 → 降级或标为「已缓解」。

### 第 3 步：汇总去重定级

1. **去重**：同一根因在多个文件重复出现时合并为一条 finding，在 `occurrences` 中列出全部位置。
2. **定级**：按本文件「严重等级定义」统一裁定，不沿用各维度文件的初步分级。
3. **交叉标注**：一个 finding 若同时命中多个维度，在 `dimensions` 数组中全部标出（例如 SSRF 同时属于注入与文件/网络处理）。
4. **打分**：按 `prompts/11-report-spec.md` 的算法计算风险分与风险等级。

### 第 4 步：产出报告

严格按 `prompts/11-report-spec.md` 生成：

1. `security-audit-report.json` — 先写 JSON，它是唯一事实源。
2. `security-audit-report.html` — 从 JSON 内容渲染，单文件、内联 CSS/JS、零外部依赖。

HTML 报告必须满足：左侧维度导航 + 右侧内容区、目录锚点平滑滚动与滚动感应（scroll-spy）、严重等级徽章与筛选、代码片段高亮、移动端适配、内容自适应行高（禁止硬编码 `height`）。

### 第 5 步：自检

交付前逐项确认：

- [ ] 每条 finding 都有真实存在的文件路径与行号，且行号可被复现
- [ ] 没有把「猜测」写成结论——不确定的一律标 `confidence: low` 并在描述中写明不确定原因
- [ ] 没有编造不存在的文件、函数、依赖版本或 CVE 编号
- [ ] 每个维度都在 `dimension_coverage` 中有明确结论（含「未发现问题」）
- [ ] `summary` 中的数量统计与 `findings` 数组实际内容一致
- [ ] 报告已写入磁盘，并已告知用户文件路径
- [ ] 未修改目标仓库的任何业务代码

## 硬性规则

1. **零执行**：不运行目标仓库的代码、脚本、构建命令，不安装其依赖。只读。
2. **零改动**：除生成的报告文件外，不写入、不删除、不重命名目标仓库内的任何文件。
3. **禁止编造**：不得虚构文件路径、行号、代码内容、依赖版本、CVE/OWASP 编号。凡是无法在代码中定位到证据的推测，必须显式标注为「推测」并给出 `confidence: low`。
4. **禁止夸大**：纯静态审计无法证明漏洞可利用。措辞用「存在风险」「疑似」，不使用「已确认可被利用」这类结论，除非代码逻辑形成完整闭环且无任何拦截。
5. **必须给证据**：每条 finding 至少包含一段真实代码片段（可截断，但不得改写）＋ 文件路径 ＋ 行号区间。
6. **边界要声明**：未覆盖的目录、未运行的维度、静态分析的固有局限，全部写入 `limitations`。
7. **密钥脱敏**：报告中若必须展示疑似真实凭证，只展示前 4 位 + `***` + 后 2 位，并提示「已泄露，需立即轮换」。绝不完整输出疑似有效的密钥。
8. **优先级**：发现 Critical 级别问题时，在回答中**先口头告知用户**，再继续完成剩余维度。

## 严重等级定义

| 等级 | 判定标准 | 典型示例 |
|------|----------|----------|
| **Critical** | 无需认证或仅需低权限即可直接获取系统控制权 / 批量数据泄露；或已确认提交进仓库的真实生产凭证 | 未授权 RCE 反序列化、SQL 注入直达全库、硬编码生产 AK/SK |
| **High** | 需要一定前置条件，但一旦触发可造成大范围数据泄露、越权、账户接管 | 存储型 XSS、水平越权读取他人数据、JWT 签名校验缺失、路径穿越读任意文件 |
| **Medium** | 影响范围受限，或需要较苛刻前置条件，或为高危漏洞的必要前置条件 | 反射型 XSS、缺少频控导致可爆破、CORS 配置过宽、弱加密算法 |
| **Low** | 信息泄露有限，或加固类问题，单独利用价值低 | 版本号泄露、错误栈暴露、缺少安全响应头 |
| **Info** | 非漏洞的最佳实践建议 | 代码规范、可观测性建议、依赖升级提示 |

**置信度（confidence）独立于严重等级：**
- `high`：代码证据完整，链路清晰，无拦截措施
- `medium`：代码证据存在，但部分上下文（如框架默认行为、外部配置）无法确认
- `low`：仅命中特征模式，未验证可达性；报告中必须写明待人工确认事项

## 输出契约

| 文件 | 位置 | 说明 |
|------|------|------|
| `security-audit-report.json` | 默认目标仓库根目录 | 唯一事实源，结构见 `prompts/11-report-spec.md` |
| `security-audit-report.html` | 同上 | 由 JSON 渲染的单文件报告，零外部依赖 |

回答用户时按以下顺序给出结论（控制篇幅，细节都在报告里）：

1. 一句话总体结论 + 风险分 + 风险等级
2. 各等级问题数量分布
3. Critical / High 问题清单（每条：编号、标题、位置、一句话影响）
4. 本次审计的边界与局限（1-2 条）
5. 报告文件路径

## 维度文件索引

| 文件 | 维度 | 核心关注 |
|------|------|----------|
| [00-index.md](prompts/00-index.md) | 总览 | 编排策略、优先级、按技术栈加权 |
| [01-secrets-and-credentials.md](prompts/01-secrets-and-credentials.md) | 密钥与凭证 | 硬编码密钥、令牌、私钥、连接串 |
| [02-injection.md](prompts/02-injection.md) | 注入类 | SQL/命令/模板/反序列化/XXE/SSTI |
| [03-authn-authz.md](prompts/03-authn-authz.md) | 认证与授权 | 认证绕过、越权、会话与令牌缺陷 |
| [04-cryptography.md](prompts/04-cryptography.md) | 密码学 | 弱算法、弱随机、TLS 校验缺失 |
| [05-dependency-supply-chain.md](prompts/05-dependency-supply-chain.md) | 依赖与供应链 | 高危组件、构建脚本、CI/CD 权限 |
| [06-sensitive-data-exposure.md](prompts/06-sensitive-data-exposure.md) | 敏感数据暴露 | 过度返回、脱敏缺失、调试接口 |
| [07-config-and-infrastructure.md](prompts/07-config-and-infrastructure.md) | 配置与基础设施 | 容器、K8s、网关、安全头、CORS |
| [08-business-logic-and-race-conditions.md](prompts/08-business-logic-and-race-conditions.md) | 业务逻辑与并发 | 金额篡改、超卖、幂等、状态机绕过 |
| [09-file-and-path-handling.md](prompts/09-file-and-path-handling.md) | 文件、路径与网络请求 | 上传、穿越、解压、SSRF |
| [10-logging-error-handling.md](prompts/10-logging-error-handling.md) | 日志与异常处理 | 异常泄露、fail-open、审计缺失 |
| [11-report-spec.md](prompts/11-report-spec.md) | 报告规范 | JSON schema、HTML 规范、打分算法 |
